/* Setup blank page controller */
angular.module('hotelAdminApp').controller('DashboardController', ['$scope', '$rootScope', '$location', '$timeout', '$http', function($scope, $rootScope, $location, $timeout, $http) {
	$scope.$on('$viewContentLoaded', function() {
        // initialize core components
        
    });
}]);
